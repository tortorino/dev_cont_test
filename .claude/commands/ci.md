Run the full CI pipeline: build all 8 WASM variants (4 production + 4 dev), generate PNG snapshots, and create test videos.

Run `make ci` and report the complete results including:
- Number of WASM files built
- PNG snapshots generated
- Video files created
- Any errors or warnings
